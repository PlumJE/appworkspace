name: dart01_var
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.6.1 

# Add regular dependencies here.
dependencies: #디펜던시 라이브러리 추가할때 실제사용할때
  # path: ^1.8.0

dev_dependencies: # 디펜던시 개발할때만 라이브러리 제한
  lints: ^5.0.0
  test: ^1.24.0
